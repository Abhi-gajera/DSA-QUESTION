#include <iostream>
#include <vector>
using namespace std;

int main()
{

   vector<int> arr{1, 2, 4, 3, 3};

   while (arr[0] != arr[arr[0]])
   {
      swap(arr[0], arr[arr[0]]);
   }
   cout << arr[0] << " ";
}
